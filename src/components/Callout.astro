---
interface Props {
    type?: "info" | "success" | "warning" | "tip";
    icon?: string;
    title?: string;
}

const { type = "info", icon, title } = Astro.props;

const styles = {
    info: {
        bg: "bg-brand-blue/10",
        border: "border-brand-blue",
        icon: "💡",
        titleColor: "text-brand-blue",
    },
    success: {
        bg: "bg-brand-green/10",
        border: "border-brand-green",
        icon: "✓",
        titleColor: "text-brand-green",
    },
    warning: {
        bg: "bg-yellow-50",
        border: "border-yellow-500",
        icon: "⚠",
        titleColor: "text-yellow-700",
    },
    tip: {
        bg: "bg-purple-50",
        border: "border-purple-500",
        icon: "💡",
        titleColor: "text-purple-700",
    },
};

const currentStyle = styles[type];
const displayIcon = icon || currentStyle.icon;
---

<div
    class={`${currentStyle.bg} ${currentStyle.border} border-l-4 p-6 rounded-r-lg my-6 shadow-sm`}
>
    <div class="flex items-start gap-4">
        {
            displayIcon && (
                <span class="text-2xl flex-shrink-0">{displayIcon}</span>
            )
        }
        <div class="flex-1">
            {
                title && (
                    <h4
                        class={`font-bold text-lg mb-2 ${currentStyle.titleColor}`}
                    >
                        {title}
                    </h4>
                )
            }
            <div class="text-gray-700 leading-relaxed">
                <slot />
            </div>
        </div>
    </div>
</div>
