---
import Layout from "../layouts/Layout.astro";

// No server-side logic needed for quiz result as it depends on client-side localStorage
---

<Layout title="Tu Diagnóstico Metabólico | Metamorfosis Real">
    <div
        class="min-h-screen bg-gray-900 text-white flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8"
    >
        <div
            id="result-container"
            class="max-w-3xl w-full space-y-8 text-center hidden"
        >
            <!-- Score Circle -->
            <div class="relative w-48 h-48 mx-auto">
                <svg class="w-full h-full transform -rotate-90">
                    <circle
                        cx="96"
                        cy="96"
                        r="88"
                        stroke="currentColor"
                        stroke-width="12"
                        fill="transparent"
                        class="text-gray-700"></circle>
                    <circle
                        id="score-circle"
                        cx="96"
                        cy="96"
                        r="88"
                        stroke="currentColor"
                        stroke-width="12"
                        fill="transparent"
                        stroke-dasharray="553"
                        stroke-dashoffset="553"
                        class="transition-all duration-1000 ease-out"></circle>
                </svg>
                <div
                    class="absolute inset-0 flex flex-col items-center justify-center"
                >
                    <span id="score-value" class="text-5xl font-extrabold"
                        >0</span
                    >
                    <span
                        class="text-sm text-gray-400 uppercase tracking-widest mt-1"
                        >Puntos IMX</span
                    >
                </div>
            </div>

            <!-- Level Title -->
            <div>
                <h1
                    id="level-title"
                    class="text-4xl sm:text-5xl font-black tracking-tight mb-4"
                >
                    <!-- Injected via JS -->
                </h1>
                <p
                    id="level-description"
                    class="text-xl text-gray-300 max-w-2xl mx-auto"
                >
                    <!-- Injected via JS -->
                </p>
            </div>

            <!-- Analysis Card -->
            <div
                class="bg-white/5 backdrop-blur-sm border border-white/10 rounded-2xl p-8 text-left"
            >
                <h3 class="text-2xl font-bold mb-4 flex items-center gap-2">
                    <svg
                        class="w-6 h-6 text-[#00C49A]"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                        ><path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-3 7h3m-3 4h3m-6-4h.01M9 16h.01"
                        ></path>
                    </svg>
                    Análisis del Experto
                </h3>
                <p id="level-analysis" class="text-gray-300 leading-relaxed">
                    <!-- Injected via JS -->
                </p>
            </div>

            <!-- CTA Section (Monetization) -->
            <div
                id="cta-section"
                class="bg-gradient-to-br from-[#00C49A]/20 to-[#007BFF]/20 border border-[#00C49A]/30 rounded-2xl p-8 relative overflow-hidden group"
            >
                <div
                    class="absolute top-0 right-0 p-4 opacity-10 group-hover:opacity-20 transition-opacity"
                >
                    <svg
                        class="w-32 h-32 text-white"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                        ><path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="1"
                            d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                    </svg>
                </div>

                <h3 class="text-2xl font-bold text-white mb-2 relative z-10">
                    Recomendación: Protocolo de Reinicio Metabólico
                </h3>
                <p class="text-gray-200 mb-6 relative z-10">
                    Basado en tu puntaje, necesitas una intervención estratégica
                    de 21 días para reactivar tu flexibilidad metabólica y
                    quemar grasa eficientemente.
                </p>

                <div
                    class="flex flex-col sm:flex-row gap-4 justify-center relative z-10"
                >
                    <a
                        href="https://buy.stripe.com/test_link_placeholder"
                        class="inline-flex items-center justify-center px-8 py-4 text-lg font-bold text-[#004d3d] bg-[#00C49A] hover:bg-[#00A885] rounded-xl transition-all shadow-lg hover:shadow-[#00C49A]/40 transform hover:-translate-y-1"
                    >
                        Obtener Plan ($9 USD)
                        <svg
                            class="w-5 h-5 ml-2"
                            fill="none"
                            stroke="currentColor"
                            viewBox="0 0 24 24"
                            ><path
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                stroke-width="2"
                                d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
                        </svg>
                    </a>
                    <a
                        href="/"
                        class="inline-flex items-center justify-center px-8 py-4 text-base font-semibold text-white border border-white/30 rounded-xl hover:bg-white/10 transition-colors"
                    >
                        Volver al Inicio
                    </a>
                </div>
                <p class="text-xs text-gray-500 mt-4 text-center">
                    Garantía de satisfacción de 30 días.
                </p>
            </div>
        </div>

        <!-- Loading State -->
        <div id="loading" class="text-center">
            <div
                class="w-16 h-16 border-4 border-[#00C49A] border-t-transparent rounded-full animate-spin mx-auto mb-4"
            >
            </div>
            <p class="text-gray-400">Analizando tus respuestas...</p>
        </div>
    </div>
</Layout>

<script>
    document.addEventListener("DOMContentLoaded", () => {
        // Simulator delay for realistic analysis feel
        setTimeout(() => {
            const scoreStr = localStorage.getItem("imx_score");

            if (!scoreStr) {
                window.location.href = "/";
                return;
            }

            const score = parseInt(scoreStr);
            document.getElementById("loading").classList.add("hidden");
            document
                .getElementById("result-container")
                .classList.remove("hidden");

            // Populate Data
            const scoreValue = document.getElementById("score-value");
            const scoreCircle = document.getElementById("score-circle");
            const levelTitle = document.getElementById("level-title");
            const levelDesc = document.getElementById("level-description");
            const levelAnalysis = document.getElementById("level-analysis");

            // Animation
            let currentScore = 0;
            const duration = 1500;
            const stepTime = Math.abs(Math.floor(duration / score));

            const timer = setInterval(() => {
                currentScore++;
                scoreValue.innerText = currentScore;
                if (currentScore >= score) clearInterval(timer);
            }, stepTime);

            // Circle Stroke Animation
            // Circumference = 2 * PI * r = 2 * 3.14159 * 88 ≈ 553
            const circumference = 553;
            const offset = circumference - (score / 100) * circumference;
            scoreCircle.style.strokeDashoffset = offset;

            // Logic Levels
            let colorClass = "";

            if (score <= 34) {
                // ROJO
                colorClass = "text-red-500";
                scoreCircle.classList.add(
                    "text-red-500",
                    "drop-shadow-[0_0_10px_rgba(239,68,68,0.5)]"
                );
                levelTitle.innerHTML = `<span class="${colorClass}">Nivel Crítico</span> (Alerta Roja)`;
                levelDesc.innerText =
                    "Tu metabolismo está rígido y dependiente de azúcar.";
                levelAnalysis.innerHTML = `
                Tu cuerpo ha perdido la capacidad de usar grasa como combustible eficiente. Esto explica tus bajones de energía, la dificultad para concentrarte y la acumulación de grasa abdominal rebelde. <br><br>
                <strong>El riesgo:</strong> Si no actúas, la resistencia a la insulina puede aumentar. Necesitas un <em>reinicio metabólico urgente</em>.
            `;
            } else if (score <= 59) {
                // AMARILLO
                colorClass = "text-yellow-400";
                scoreCircle.classList.add(
                    "text-yellow-400",
                    "drop-shadow-[0_0_10px_rgba(250,204,21,0.5)]"
                );
                levelTitle.innerHTML = `<span class="${colorClass}">Nivel Precaución</span> (Estancamiento)`;
                levelDesc.innerText =
                    "Metabolismo lento con signos tempranos de resistencia.";
                levelAnalysis.innerHTML = `
                Aunque tienes días buenos, tu cuerpo lucha para mantener niveles estables de energía. Es probable que sientas sueño después de comer o antojos dulces por la tarde. <br><br>
                <strong>La oportunidad:</strong> Estás a tiempo de restaurar tu flexibilidad metabólica antes de que la resistencia se vuelva crónica.
            `;
            } else if (score <= 84) {
                // VERDE
                colorClass = "text-green-400";
                scoreCircle.classList.add(
                    "text-green-400",
                    "drop-shadow-[0_0_10px_rgba(74,222,128,0.5)]"
                );
                levelTitle.innerHTML = `<span class="${colorClass}">Nivel Optimizado</span> (Saludable)`;
                levelDesc.innerText =
                    "Tienes una buena base metabólica, pero puedes mejorar.";
                levelAnalysis.innerHTML = `
                ¡Buen trabajo! Tu cuerpo maneja bien la energía, pero aún no has desbloqueado tu máximo potencial. Probablemente ya haces ejercicio o cuidas tu dieta, pero te falta el "ajuste fino" para quemar grasa sin esfuerzo. <br><br>
                <strong>Siguiente paso:</strong> Enfócate en la periodización de carbohidratos y ayuno profundo para llegar a nivel Élite.
            `;
            } else {
                // ELITE
                colorClass = "text-blue-400";
                scoreCircle.classList.add(
                    "text-blue-400",
                    "drop-shadow-[0_0_10px_rgba(96,165,250,0.5)]"
                );
                levelTitle.innerHTML = `<span class="${colorClass}">Nivel Élite</span> (Flexibilidad Total)`;
                levelDesc.innerText =
                    "Eres una máquina de quemar grasa y eficiencia.";
                levelAnalysis.innerHTML = `
                Tu salud metabólica está en el 1% superior. Tu cuerpo cambia entre quemar glucosa y grasa sin problemas. Tienes energía estable, claridad mental y un físico fuerte. <br><br>
                <strong>Tu reto:</strong> Mantener este rendimiento y enfocarte en longevidad y regeneración celular (autofagia profunda).
            `;
                // Hide purchase CTA for Elite or change text
                document.getElementById("cta-section").innerHTML = `
                <div class="text-center">
                    <h3 class="text-2xl font-bold text-white mb-4">¡Felicidades!</h3>
                    <p class="text-gray-300 mb-6">Sigue aprendiendo con nuestros artículos avanzados para biohackers como tú.</p>
                    <a href="/posts" class="inline-flex items-center justify-center px-8 py-4 text-lg font-bold text-white bg-[#007BFF] hover:bg-[#0069d9] rounded-xl transition-all">Ver Artículos Avanzados</a>
                </div>
            `;
            }
        }, 800);
    });
</script>
